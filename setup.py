from setuptools import setup, find_packages

setup(
    name='test_hello_module',
    version='1.2.0',
    packages=find_packages(),
    install_requires=[],
    # install_requires=load_requirements('requirements.txt'),
)
